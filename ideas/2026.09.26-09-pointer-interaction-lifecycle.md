# 09 - Sparkline pointer interaction lifecycle

Reviewed on 2026-09-26 against master `41ddd329a3460ef1dd7716826254fd5435271312`, version `5.4.7-dev.32`.
Feature issue: #745. Feature branch: `745-sparkline-pointer-interaction-lifecycle`.
Subissues: #746 current routing; #747 SVG and interaction cleanup; #748 tooltip synchronization and visual build; #749 permanent acceptance.

## 1. Goal

Keep pointer/tooltip behaviour correct across graph-type changes, DOM replacement, drag lifecycle, data becoming empty, disconnect and config replacement without rewriting Safari-sensitive routing.

Historical charts show an indicator for the selected time. Real-time charts keep their tooltip but have no selection indicator: their chart already represents the one current value.

## 2. Prerequisites

- Plan 03 explicit current data/empty state;
- Plan 05 permanent cleanup path;
- Plan 08 pointer events excluded from normal data/geometry flow.

## 3. Current-code validation

Reproduce/check line -> radial runtime switching, handler closures, actual SVG node identity, window-level drag listeners, pointer RAF and tooltip state. Run WebKit baseline before broad event changes.

Current review confirms stale captured chart family, missing pointercancel handling, uncancelled drag frames after pointerup, old-node listeners after SVG replacement, and cartesian-only tooltip refresh after accepted data updates. Plan 05 already owns disconnect/replacement propagation and node cleanup; reuse that foundation.

## 4. Scope

- stable handler identity without capturing mutable old graph mode/series;
- handlers attached to the current SVG node exactly once;
- drag start/move/end/cancel;
- global listener + RAF ownership;
- disconnect/replacement ends active interaction through Plan 05 cleanup;
- data/config change clears invalid selection/tooltip.
- real-time tooltips remain available without a selection indicator.

## 5. Out of scope

- changing History/data/geometry flow;
- creating an interaction framework;
- broad event API modernization.

## 6. Required behaviour

Stored callbacks read current runtime graph family/series at execution time. “Handlers attached” must be tied to actual node reference, not merely a boolean that survives node replacement.

Pointerup, pointercancel, disconnect and replacement use the same concrete interaction-end cleanup.

Touch cancellation uses the same cleanup. Ending an interaction cancels its frames, removes global listeners, clears selection and hides tooltip/highlighting without another pointer calculation. Callbacks have stable identity for their tool lifetime.

Valid live data preserves active interaction and recomputes selection from the last pointer coordinates using current geometry. Refresh DOM references after rendering. Chart-type, source or Series-composition changes and accepted empty data end the obsolete interaction; ordinary paint updates do not.

## 7. Implementation sequence

1. Add line/radial and survivor reproductions.
2. Inventory captured mutable variables in stored callbacks.
3. Make graph-family/series lookup current at event/frame execution.
4. Track actual SVG node receiving handlers.
5. Consolidate concrete pointer-end cleanup enough for all endings.
6. Own/cancel pointer RAF and window listeners locally.
7. clear tooltip/selection on Plan 03 empty/config replacement.
8. Run Chromium + mandatory WebKit lifecycle cases.

Implementation runs as four subissues, each branching from and merging into the feature branch:

1. #746 current routing and minimal reproductions. Result: current chart-family selection for hover, drag and live-data tooltip refresh.
2. #747 SVG ownership and shared interaction-end cleanup. Result: no duplicate bindings or surviving old-node/frame interactions.
3. #748 tooltip/data synchronization and real-time indicator behavior. Result: responsive live tooltips and historical selection; build for user visual verification.
4. #749 permanent acceptance. Result: focused regressions, integration review and accepted plan/software/build.

Close each subissue after verified feature-branch merge. Master remains untouched until explicit user approval.

## 8. Permanent tests

Complete `TESTING.md` pointer matrix and listener/RAF add-remove balance where test DOM supports it.

Include radial barcode, state bands and real-time bar/equalizer/graded; unchanged/replaced SVG; queued frames followed by leave/end/cancel/disconnect/replacement; live geometry/empty/partial-Series transitions. Verify zero history requests, reaggregation, normal geometry rebuilds and setHass re-entry from pointer movement. Use real neighboring FHS owners and controlled DOM/network/clock boundaries.

Sol xhigh owns architecture and integration. Luna Max writes bounded tests. Minimal failing reproductions precede fixes; broad acceptance follows user visual verification. Final gates: full Node suite, lint/build, affected Chromium and required WebKit tests. Avoid repeated builds without source changes or failures. Existing unrelated WebKit fixture/tangent failures are diagnosed separately, not hidden by changed expectations.

## 9. Definition of Done

- pointer behaviour always follows current graph type/data;
- old closures do not retain obsolete chart state;
- rerender does not duplicate listeners and a replaced node receives handlers;
- drag listeners/RAF always end on pointerup/cancel/disconnect/replacement;
- empty/reconfiguration clears invalid tooltip state;
- pointer interaction never enters history/data/setHass flow;
- Safari-safe route remains covered.
- real-time charts retain tooltips without selection indicators.
- functional JSDoc and inline comments explain registration, cleanup and data/DOM synchronization.

## 10. Guarantees for following plans

Pointer lifetime is closed. Final cleanup may remove obsolete guards/comments but must not redesign the event route without a new reproduced defect.

## 11. Source material

Replaces old Plan 10; uses Plan 05 instead of reimplementing replacement cleanup and Plan 03 instead of inventing tooltip readiness.

Keep this accepted dated plan with its software commits. Shared obligations come from `ideas/refactor/00-master-refactor-plan.md` and `ideas/refactor/TESTING.md`; current Plan 14 owns CI/release changes, which remain out of scope here. Preserve the user's separate master-plan edits.

## 12. Implementation status

- #746 completed through PR #750: callbacks use the current chart family; SVG entry selects from coordinates; dated plan accompanies the source.
- #747 completed through PR #751: constructor-bound handlers, actual-node registration, shared gesture cleanup and revoked-frame cancellation.
- #748 completed through PR #752: active tooltips reproject onto current data/DOM; real-time charts omit selection indicators. The source and 14 bounded Node pointer regressions are in the feature branch.
- #749 acceptance completed after the user's explicit approval to merge Plan 09 before addressing CPU load: all 412 Node tests, lint and the full build pass. Seven affected lifecycle/history integration regressions and four dedicated pointer browser regressions pass in both Chromium and WebKit. The browser cases cover actual SVG hover, current line/radial routing, replaced-node cleanup, cancellation/disconnect/reconnect, and real-time tooltips without an indicator.

The user reported periodic CPU spikes on a dashboard with many sparklines while this work was underway. The history-decision message is debug-gated and records checks, not necessarily requests; the user observed that those messages do not coincide with CPU spikes. A controlled 24-card Chromium comparison found substantial processing cost on ordinary sensor changes in both master and the pointer build, with debug disabled and no new requests or pointer calculations. Complete history conversions, duplicate single-series statistics work and full graph-array presentation signatures account for substantial measured cost. The exact relationship to the user's recurring peaks remains to be confirmed. Plan 09 changes none of that processing or request scheduling; the separately authorized CPU fix is tracked in #753. Empty-bin tooltip behavior and sample-timestamp semantics remain outside that CPU fix.
