# Horseshoe path-engine contract and parity fixtures

Date: 2026-09-05  
Parent: [#568](https://github.com/AmoebeLabs/flex-horseshoe-card/issues/568)  
Subissue: [#569](https://github.com/AmoebeLabs/flex-horseshoe-card/issues/569)

## Purpose

This document records the visible horseshoe behavior preserved by the generic
path engine. The final implementation must preserve these results unless a
later issue explicitly changes the product contract.

This inventory does not make runtime changes. It identifies the representative
automated assertions and visual cases that the final path engine must keep green.

## Configuration boundary

The current horseshoe accepts the established `layout.horseshoes` configuration.
`layout.horseshoes_v2` and the root-level compatibility fields reach the same
runtime today, but they are migration inputs rather than three different visual
contracts. The canonical gauge normalizes configuration once and then feeds one
behavior model.

The minimum useful gauge consists of an entity, position, radius, and scale:

```yaml
layout:
  horseshoes:
    - entity_index: 0
      xpos: 50
      ypos: 50
      radius: 40
      horseshoe_scale:
        min: 0
        max: 100
```

## Parity matrix

| Area | Current user-visible contract | Representative automated coverage | Final parity fixture |
| --- | --- | --- | --- |
| Arc geometry | `radius`, `tickmarks_radius`, `arc_degrees`, `start_angle`, `rotate`, and `flip` place one gauge around its configured center. A configured 360-degree arc remains visually complete. | `tests/path-generators.test.js`, `tests/path-geometry.test.js`, and `tests/horseshoe-path-adapter.test.js`. | 270-degree browser comparison plus a complete 360-degree unit case. |
| Scale mapping | `linear`, `spline`, and `splineorg` map values to positions. Values outside the scale remain at its endpoints. | `tests/path-ranges.test.js`. | Linear and non-linear mappings use the same normalized path range. |
| Normal state | The state starts at scale minimum and ends at the current value. | `tests/path-ranges.test.js` and `tests/horseshoe-path-adapter.test.js`. | Values map to one path-independent state range. |
| Bidirectional state | `bidirectional`, `bidirectional_symmetrical`, and `bidirectional_linear` start at zero and extend along the matching signed branch. One-sided scales retain the relevant half. | `tests/path-ranges.test.js` and `tests/horseshoe-path-adapter.test.js`. | Negative and positive ranges retain the configured zero position. |
| Absolute state | `absolute` draws signed magnitude over the complete path while retaining the signed value for colors and displayed state. Its active scale, ticks, and labels follow the active sign. | `tests/path-ranges.test.js`, `tests/horseshoe-state.test.js`, and `tests/horseshoe-path-adapter.test.js`. | A sign change rebuilds the complete active tick and label branch. |
| Numeric state styles | `fixed`, `autominmax`, `colorstop`, `colorstopinterpolated`, `colorstopsegments`, `minmaxgradient`, `lineargradient`, and `colorstopgradient` select the current state paint behavior. | `tests/horseshoe-path-adapter.test.js`, `tests/path-ranges.test.js`, and `tests/path-gradient-renderer.test.js`. | Solid, segmented, full-gradient, and current-gradient contracts use the same path. |
| Scale styles | `fixed`, `none`, `colorstopsegments`, `lineargradient`, and `colorstopgradient` control the complete scale independently from the current state. | `tests/horseshoe-path-adapter.test.js`, `tests/path-ranges.test.js`, and `tests/path-gradient-renderer.test.js`. | Scale paint remains independent from state paint. |
| String states | Ranked string color stops produce numeric geometry while exact Home Assistant states retain their labels, relations, and colors. `stringstate_mode` highlights one segment; `stringstate_level` highlights the range through the active segment. | `tests/string-color-stops.test.js`, `tests/path-ranges.test.js`, and `tests/horseshoe-labels.test.js`. | State relations and label styles remain attached to each mapped segment. |
| Segments and gaps | Internal gaps are split equally between adjacent ranges. The first and last range keep the true path endpoints. State `segment_gap` overrides the color-stop gap. | `tests/path-ranges.test.js` and `tests/horseshoe-state.test.js`. | Exact normalized boundaries preserve both complete endpoints. |
| Endpoint caps | Scale and state have independent `linecap.start` and `linecap.end`. Internal segmented endpoints remain butt caps; only the complete visible start/end receive configured caps. | `tests/path-renderer.test.js` and `tests/path-ranges.test.js`. | Every mixed cap combination uses the same mask layer. |
| Background layers | Horseshoe, label, and tick backgrounds may be fixed, segmented, or gradient paths with independent offset, width, gap, and styles. Layer opacity is composed once. | `tests/path-renderer.test.js` and `tests/horseshoe-path-adapter.test.js`. | Backgrounds remain separate path layers behind scale and state. |
| Major/minor ticks | Major and minor ticks are independently visible, sized, offset, shaped, styled, and colored. Their values use the same active scale branch as the state. | `tests/horseshoe-path-adapter.test.js` and `tests/path-elements.test.js`. | Major and minor layers share measured points and normals. |
| Labels | `none`, `minmax`, `minmax0`, `colorstop`, `ticks_major`, `both`, `segment`, and `stringstate` select label sources. `distance_min` suppresses labels that are too close. Labels can remain horizontal or follow the path. | `tests/horseshoe-labels.test.js`, `tests/horseshoe-path-adapter.test.js`, and `tests/path-elements.test.js`. | Label selection and final measured placement are tested independently. |
| Label backgrounds and badges | A label background is one path layer. `label_badges` adds one independently styled badge behind every visible label. | `tests/path-elements.test.js` and `tests/horseshoe-path-adapter.test.js`. | Badges use final label coordinates without transforming their text. |
| Group transforms | Group rotation and scale combine with item rotation and flip around their configured centers. Labels remain readable because their final coordinates are calculated separately. | `tests/path-geometry.test.js`, `tests/path-elements.test.js`, and `tests/horseshoe-path-adapter.test.js`. | Rotated and reflected geometry leaves horizontal label text untransformed. |
| State animation | Numeric state changes interpolate only the displayed state. Disabled animation publishes the target immediately. A new target replaces an active animation without rebuilding static layers. | `tests/path-animator.test.js` and `tests/horseshoe-path-adapter.test.js`. | Targeted state updates retain measured geometry and static elements. |
| Layer order | Background bands remain behind scale/state; label and tick backgrounds remain behind their content; state updates do not reorder static layers. | `tests/path-renderer.test.js`, `tests/path-elements.test.js`, and `tests/horseshoe-path-adapter.test.js`. | Renderer structure and static-layer reuse keep ordering stable. |

## Markers

The current horseshoe has no standalone path marker configuration or rendered
marker layer. Labels, badges, and tick shapes must not be reclassified as a
marker to claim parity. Path markers are therefore a path-engine addition, not
an original horseshoe behavior. Their contract starts in issue #580 and uses
measured point, tangent, and normal operations rather than arc-specific math.

## Representative screenshot set

The path-engine test set covers the following representative cases without
creating one screenshot for every configuration permutation.

| Fixture | Content | Main regressions detected |
| --- | --- | --- |
| `horseshoe-parity-basic` | Fixed scale and state at min, midpoint, and max; 270 and 360 degrees. | Arc endpoints, clamping, width, center, complete circle. |
| `horseshoe-parity-paint` | All state styles; segmented and gradient scales; mixed caps and gaps. | Colors, range boundaries, seams, cap ownership, gradient direction. |
| `horseshoe-parity-signed` | Normal, bidirectional variants, and absolute mode with negative/zero/positive values. | Zero placement, direction, active signed branch, tick and label alignment. |
| `horseshoe-parity-string` | Ranked string mode and level gauges with segment labels. | State ranking, active range, translated label placement and styling. |
| `horseshoe-parity-annotation` | Major/minor ticks, horizontal/arc labels, label background, badges, transforms. | Offsets, readability, transform composition, layer order. |
| `horseshoe-parity-animation` | Running and interrupted numeric transitions. | Static-layer flicker, stale paths, wrong final target, rerender regressions. |

## Migration gate

The path engine may replace a current horseshoe layer only when:

1. its unit-level assertions remain green;
2. the corresponding browser parity fixture matches the approved baseline;
3. the configuration produces the same visible result through canonical
   `layout.horseshoes` configuration;
4. no new path generator contains horseshoe-specific value, color, label, tick,
   badge, or animation decisions.

The final cutover in #584 requires all rows in this matrix. A deliberate product
change must be documented and reviewed separately rather than hidden inside the
path-engine migration.
